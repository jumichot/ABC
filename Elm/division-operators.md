#Elm division operators

There is two different operators for division in Elm.
The general / for dividing floats, and the special operator // for using with Int

```
> (/)
<function> : Float -> Float -> Float
> (//)
<function> : Int -> Int -> Int
```
